{{> head }}

    <body>
        
    {{#if main }}
        {{> navbar}}
    {{/if}}
        <div class="main_container">
            
            {{{ body }}}
        </div>
        {{> footer}}
    </body>
    {{> background}}
</html>